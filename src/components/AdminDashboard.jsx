import React from 'react';

    const AdminDashboard = () => {
      return (
        <div>
          <h1>Admin Dashboard</h1>
          <p>Manage all users, campaigns, rewards, and analytics.</p>
        </div>
      );
    };

    export default AdminDashboard;
